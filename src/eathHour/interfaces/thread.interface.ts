/* eslint-disable prettier/prettier */

export interface Thread {
    threadId: string;		 
    subForumId: string;	 
    userId: string;		 
    upvotes: number;		 
    threadTitle: string;		 
    threadBody: string;		 
    threadAttachment: string;	
}