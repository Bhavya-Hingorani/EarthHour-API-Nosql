/* eslint-disable prettier/prettier */

export interface SubThread {
    subThreadId: string;		 
    threadId: string;	 
    userId: string;		 
    upvotes: number;		 		 
    subThreadBody: string;		 
}